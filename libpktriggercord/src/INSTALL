pkTriggerCord installation (you need root privileges for 'make install')

make clean
make
make install

If you don't need the GUI, you can compile only the commandline
interface:

make cli

If you'd like to debug the program you don't have to recompile
just use --debug switch

